{
  "name": "antd-platform",
  "version": "0.2.4",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "patch-package && npm run build --workspace @platform/shared-types",
    "release": "node scripts/release.mjs"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1"
  },
  "devDependencies": {
    "drizzle-orm": "^0.45.2",
    "patch-package": "^8.0.1",
    "pg": "^8.22.0"
  }
}
